FROM node:latest

# REQUIRED ARG PROVIDED BY DOCKER COMPOSE BUILD
ARG PORT=3000

# REQUIRED ARG PROVIDED BY DOCKER COMPOSE RUN
ARG PGDATABASE=LIVE

EXPOSE ${PORT}

RUN mkdir /nodeAppPostPg
ADD . /nodeAppPostPg

WORKDIR /nodeAppPostPg

RUN npm install

ENTRYPOINT ["npm", "start"]