export { DbDefsLegacy };

class DbDefsLegacy {
  readonly CANCEL_RIDE_OFFER_FUNCTION = 'cancel_drive_offer($1, $2)';
  readonly REJECT_RIDE_FUNCTION = 'reject_ride($1, $2)';
  readonly CONFIRM_RIDE_FUNCTION = 'confirm_ride($1, $2)';
}
